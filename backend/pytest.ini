[pytest]
# Scoped config for the backend multi-tenancy / auth suite. These tests are
# synchronous (FastAPI TestClient), so no asyncio plugin is required. Having a
# pytest.ini here makes backend/ the rootdir for `pytest tests`, so the root
# pytest.ini (asyncio_mode=auto, for the packages/apps suites) is not picked up.
testpaths = tests
