[pytest]
# Root config so the combined multi-package invocation used by `make test` and
# CI resolves a single rootdir with async support. Individual packages also set
# this in their pyproject.toml for isolated runs.
asyncio_mode = auto
